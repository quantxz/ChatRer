{
  "name": "chatrer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "prisma generate",
    "build": "prisma generate && next build",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "generate": "npx prisma init",
    "start": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@types/cors": "^2.8.13",
    "@types/node": "^20.5.0",
    "@types/socket.io": "^3.0.2",
    "cors": "^2.8.5",
    "ejs": "^3.1.9",
    "socket.io": "^4.7.2",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@prisma/client": "^5.1.1",
    "prisma": "^5.1.1"
  }
}
